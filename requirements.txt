tkinter==8.6.10  # Note: tkinter comes with Python, so you might not need to install it via pip
financedatareader==0.9.31
plotly==5.3.1
pandas==1.3.3
TA-Lib==0.4.0  # Note: TA-Lib installation might require additional setup, as it's a C library
numpy==1.21.2
openpyxl==3.0.9 